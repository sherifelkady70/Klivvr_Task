package com.route.cities.search

interface TrieInterface {

//    suspend fun insert(word: String)
//
//    suspend fun search(prefix: String): List<String>
//
//    suspend fun findWords(node: TrieNode, prefix: String, results: MutableList<String>)
}